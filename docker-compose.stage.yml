services:
  redis:
    container_name: redis
    image: redis:8.10.0-alpine
    restart: unless-stopped
    volumes:
      - redis-data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    networks:
      - template-server-network

  server-stage:
    depends_on:
      redis:
        condition: service_healthy
    container_name: template-server-stage
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
      - /app/node_modules
    ports:
      - 8081:8080
    environment:
      - NODE_ENV=staging
      - PORT=8080
      - REDIS_HOST=redis
    env_file:
      - .env
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'node -e "fetch(''http://127.0.0.1:8080/api/v1/health'').then((response) => { if (!response.ok) process.exit(1); }).catch(() => process.exit(1))"',
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - template-server-network

networks:
  template-server-network:
    driver: bridge

volumes:
  redis-data:
