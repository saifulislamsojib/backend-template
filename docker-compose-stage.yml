services:
  backend_stage:
    container_name: backend_stage
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - 8081:8081
    environment:
      - NODE_ENV=staging
      - PORT=8081
    env_file:
      - .env
